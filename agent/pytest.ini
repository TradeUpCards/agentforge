[pytest]
# Custom marks registration.  Without this, pytest warns "Unknown pytest.mark.X".
markers =
    live: marks tests as live integration tests that hit real APIs (opt-in with --live)
    asyncio: marks tests as requiring the asyncio event loop
