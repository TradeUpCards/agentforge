"""Integration tests — require live services (ANTHROPIC_API_KEY, Docling, etc.).

Tests in this package are skipped in CI unless the required env vars are set.
See individual test files for skip conditions.
"""
